# site/ に置いてよいもの

**このフォルダは GitHub Pages で全世界に公開される。**
そして **git の履歴は消せない。** 一度コミットしたものは、あとで削除しても他人が読める。

だから「あとで消せばいい」が通用しない。**置く前に止める**しかない。

---

## ルール（機械が強制する）

`git commit` のたびに `公開前チェック.py` が自動で走り、違反があればコミットを止める。
設置場所は `.git/hooks/pre-commit`。

### ① 置いてよい拡張子だけを通す（許可リスト方式）

```
.html .css .js .svg .png .jpg .jpeg .webp .ico .woff2
```

**これ以外は、理由を問わず止まる。**

禁止リスト方式（危ないものを列挙する）は必ず漏れるので採らない。
`.py` `.md` `.json` `.csv` `.txt` はすべて止まる。データもスクリプトもメモも、ここには置かない。

### ② 中身も検査する

許可した拡張子でも、HTMLにキーを書くことはできてしまうので中身を見る。

| 止まるもの | なぜ |
|---|---|
| 個人アカウント名（`r_orimoto` など） | 運営者の特定に繋がる |
| ローカルの絶対パス（`/Users/…`） | 利用者名が出る |
| ミサキの生年月日 | 設定の内部値。公開しない（日干「辛」までは可） |
| 四柱の併記（年柱・月柱・日柱） | 生年月日が逆算できる |
| APIキー・トークン | 言うまでもない |
| 売上・購入者・顧客データ | 事業の内部情報 |
| 非公開スクリプトの名前 | 内部構成が読める |

### ③ 外部通信が増えたら知らせる

判定ページは「**生年月日を送らない**」が売りなので、通信を足したら必ず気づけるようにしてある。

`fetch` `sendBeacon` `<script src>` などが新しく入ると、確認を求めて止まる。
問題ないと確認できたら `公開前チェック.py` の `NET_ALLOW` に追記する。

> 現在の許可：`goatcounter.com`（アクセス数の集計。生年月日は送っていない）

---

## 置いてよいもの / いけないもの

| | |
|---|---|
| ✅ | 判定ページ、相関図、そこで使うCSS・JS・画像・フォント |
| ✅ | `CNAME`（独自ドメインを当てるとき） |
| ❌ | 鑑定文のテンプレ、プロンプト |
| ❌ | 売上・購入者・PVなどの実データ |
| ❌ | 設定の正本、記事の原稿、調査レポート |
| ❌ | `.py` `.json` `.csv` などのファイル全般 |

**判断に迷ったら置かない。** 置かなくて困ることは後から直せるが、置いて困ることは直せない。

---

## 手で確認したいとき

```bash
cd その他_既存事業/占い/site
python3 公開前チェック.py --all     # いまある全ファイルを検査
python3 公開前チェック.py           # ステージ済みのものだけ検査
```

## どうしても通したいとき

```bash
git commit --no-verify
```

**使う前に、それが履歴に永久に残ってよいものか考えること。**

---

## 万が一、入れてしまったら

削除コミットでは消えない。履歴に残る。

**リポジトリごと作り直すのが、いちばん確実で速い。**

```bash
gh repo delete ol-uranai/ol-uranai.github.io
```

そのうえで `site/` から作り直す。公開しているのは2ファイルだけなので、作り直しは数分で終わる。
